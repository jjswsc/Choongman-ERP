/** 회원 포털 홈 — 참고 시안(Choongman 멤버십 앱) 비율·간격·이미지 규격 단일 소스 */

/** 화면 좌우 패딩 (시안 ≈16px) */
export const MP_HOME_SCREEN_PX = 'px-4'

/** 섹션 간 세로 간격 (시안 ≈20px) */
export const MP_HOME_SECTION_GAP = 'space-y-5'

/** 카드 모서리 (시안 ≈18px) */
export const MP_HOME_CARD_RADIUS = 'rounded-[18px]'

/** 멤버십 카드 가로:세로 (시안 — 넓고 낮은 카드) */
export const MP_HOME_MEMBERSHIP_ASPECT = 100 / 56

/** 프로모 배너 높이 (시안 — 16:9보다 낮음) */
export const MP_HOME_HERO_HEIGHT = 'h-[9.5rem]'

/** 혜택 카드 — 한 화면에 3장 (gap 12px) */
export const MP_HOME_PRIVILEGE_CARD_WIDTH = 'w-[calc((100%-1.5rem)/3)]'

/** 스탬프 슬롯 (시안 — 10개 한 줄) */
export const MP_HOME_STAMP_SLOT_SIZE = 'h-8 w-8'

/** 스탬프 카드 우측 음식 이미지 */
export const MP_HOME_STAMP_FOOD_SIZE = 'h-[7.25rem] w-[7.25rem]'

/** 홈·주문 탭 프로모 히어로 배너 — 가로형 (≈9.5rem 높이, object-cover object-right) */
export const MP_HOME_HERO_ASPECT_W = 12
export const MP_HOME_HERO_ASPECT_H = 5
export const MP_HOME_HERO_MIN_WIDTH = 1200
export const MP_HOME_HERO_MIN_HEIGHT = 500

/** 홈 팝업 시트 상단 이미지 */
export const MP_HOME_POPUP_ASPECT_W = 4
export const MP_HOME_POPUP_ASPECT_H = 5
export const MP_HOME_POPUP_MIN_WIDTH = 720
export const MP_HOME_POPUP_MIN_HEIGHT = 900

/** 주문 탭 신메뉴 정사각 카드 */
export const MP_HOME_NEW_MENU_ASPECT_W = 1
export const MP_HOME_NEW_MENU_ASPECT_H = 1
export const MP_HOME_NEW_MENU_MIN_SIZE = 720
