# OAuth 인증 정보 (clasp-clean-remote용, GAS에 업로드 금지)
credentials.json
# Node.js 유틸 스크립트 (require 미지원 Apps Script에 업로드 금지)
scripts/**
# 백업 폴더 제외 (appsscript.json 중복 푸시 방지)
backup_before_680/**
old_files_backup/**
# 의존성 제외
node_modules/**
# 아이콘 원본 파일 (배포에는 IconData.html만 사용)
assets/**
# Vercel 앱 (GAS 배포와 무관 - 70+개 파일로 인한 푸시 멈춤 방지)
vercel-app/**
# Next.js 앱 및 빌드 결과물 (self is not defined 오류 방지)
chungman-erp/**
erp-ui/**
**/.next/**
